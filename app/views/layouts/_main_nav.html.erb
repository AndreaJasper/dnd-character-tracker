<nav class="flex items-center justify-between flex-wrap p-6">
  <div class="flex items-center flex-shrink-0 text-gray mr-6">
    <a href="/">
      <span class="font-semibold text-xl tracking-tight text-white">DnD Character Tracker</span>
    </a>
  </div>
  <div class="block lg:hidden">
    <button class="flex items-center px-3 py-2 border rounded text-white border-white hover:text-white hover:border-white">
      <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
    </button>
  </div>
  <div>
    <% if current_user %>
      <%= link_to users_path do %>
        <%= image_tag "https://via.placeholder.com/50", alt: "profile image", class: "profile-img" %>
      <% end %>
      <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "block lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
    <% else %>
      <%= link_to 'Login', new_user_session_path, class: "block button primary-btn lg:inline-block lg:mt-0 hover:text-white" %>
      <%= link_to 'Register', new_user_registration_path, class: "block mt-4 mx-4 button secondary-btn lg:inline-block lg:mt-0 hover:text-white" %>
    <% end %>
  </div>
</nav>
